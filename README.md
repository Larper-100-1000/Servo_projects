# Servo_projects
I will be making some projects slowly scaling in difficulty with the main components being servos. This will theach me alot about servos and also about coding and using different components with the servos.

I have a servo that moves on it's own that uses the millis() function to control speeds with the random function the positions is also random, the milli's func is usefull because it doesn't block other code as when using the delay function.

In the future i want to use a camera or another sensor to detect a target and make a laser point at the target.

The next step is to make the servo spin left to right with the millis function and make the amount of left to right movements controlable.
